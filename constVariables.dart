void main() {
  // const : compile-time constant
  const name = '석현';
  // name = 'test';

  // API Key값과 같이 컴파일 전에 할당되어 있을때 사용
  const API_KEY = '12341234';

  // fetchApi() 처럼 api 호출 이후에 할당되는 값에서는 사용이 불가
}
